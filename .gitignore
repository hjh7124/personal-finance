__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
.pytest_cache/

# ─────────────────────────────────────────────────────────────
# 실제 원장은 기본적으로 추적하지 않는다.
#
# 이 리포지토리가 public이면 지출 내역과 잔액이 그대로 공개된다.
# 리포지토리를 private으로 바꿨다면 아래 두 줄을 지워라.
# 그때부터 자산 변화가 git 이력으로 자동으로 쌓인다.
# (data/example/ 의 예시 데이터는 계속 추적된다.)
# ─────────────────────────────────────────────────────────────
/data/balances.csv
/data/expenses.csv
/data/income.csv
